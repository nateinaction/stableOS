# Create the persistent Nix store directory on first boot. /var is empty,
# machine-local state, so this cannot be baked into the image; nix.mount then
# bind-mounts /var/nix onto /nix.
d /var/nix 0755 root root -
